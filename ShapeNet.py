
import os
import sys
import pandas as pd
import dataset
from filecmp import dircmp


class ShapeNet:
    def __init__(self):
        self._cur_index = 0
        self._=
        self._length=len()

    def next_batch(self, batch_count):
        self._cur_index += batch_count






if __name__ == '__main__':
    shapenet = ShapeNet()
    shapenet.get_paths()
